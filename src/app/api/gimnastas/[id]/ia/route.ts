import {NextRequest,NextResponse} from 'next/server';import {z} from 'zod';import {getAuthenticatedClub} from '../../../../../lib/supabase-server';import {aiSuggestionSchema} from '../../../../../lib/ai-suggestions/schema'
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!z.string().uuid().safeParse(id).success)return NextResponse.json({error:'Gimnasta inválida'},{status:400});const input=aiSuggestionSchema.safeParse(await request.json().catch(()=>({})));if(!input.success)return NextResponse.json({error:'Sugerencia inválida',details:input.error.flatten()},{status:400});const {supabase,user,clubId,error}=await getAuthenticatedClub();if(error||!user||!clubId)return NextResponse.json({error:error||'No autenticado'},{status:401});const {data:gymnast}=await supabase.from('atletas').select('id').eq('id',id).eq('club_id',clubId).is('deleted_at',null).maybeSingle();if(!gymnast)return NextResponse.json({error:'Gimnasta no autorizada'},{status:403});const v=input.data;const {data,error:insertError}=await supabase.from('sugerencias_ia').insert({club_id:clubId,atleta_id:id,tipo:v.type,entrada_contexto:v.context,propuesta_original:v.originalProposal,propuesta_editada:v.editedProposal,fundamento:v.rationale,modelo:v.model,estado:'pendiente_aprobacion',generado_por:user.id}).select('id').single();if(insertError)return NextResponse.json({error:insertError.message},{status:400});return NextResponse.json({suggestion:data},{status:201})}
