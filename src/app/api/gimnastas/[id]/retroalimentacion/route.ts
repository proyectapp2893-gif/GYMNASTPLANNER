import {NextRequest,NextResponse} from 'next/server';import {z} from 'zod';import {getAuthenticatedClub} from '../../../../../lib/supabase-server';import {feedbackSchema} from '../../../../../lib/feedback/schema'
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!z.string().uuid().safeParse(id).success)return NextResponse.json({error:'Gimnasta inválida'},{status:400});const input=feedbackSchema.safeParse(await request.json().catch(()=>({})));if(!input.success)return NextResponse.json({error:'Retroalimentación inválida',details:input.error.flatten()},{status:400});const {supabase,user,clubId,error}=await getAuthenticatedClub();if(error||!user||!clubId)return NextResponse.json({error:error||'No autenticado'},{status:401});const {data:gymnast}=await supabase.from('atletas').select('id').eq('id',id).eq('club_id',clubId).is('deleted_at',null).maybeSingle();if(!gymnast)return NextResponse.json({error:'Gimnasta no autorizada'},{status:403});const v=input.data;const {data,error:insertError}=await supabase.from('retroalimentaciones').insert({club_id:clubId,atleta_id:id,sesion_id:v.sessionId,elemento_id:v.elementId,comentario_entrenador:v.coachComment,indicacion_tecnica:v.technicalCue,explicacion:v.explanation,percepcion_gimnasta:v.gymnastPerception,nivel_comprension:v.understandingLevel,proximo_foco:v.nextFocus,visible_familia:v.visibleToFamily,created_by:user.id}).select('id').single();if(insertError)return NextResponse.json({error:insertError.message},{status:400});return NextResponse.json({feedback:data},{status:201})}
