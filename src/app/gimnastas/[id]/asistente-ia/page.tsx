import AiHistoryGenerator from '../../../../components/gymnasts/AiHistoryGenerator';import AiSuggestionReview from '../../../../components/gymnasts/AiSuggestionReview';import {getAiSuggestions} from '../../../../lib/gymnasts/server'
export default async function AiPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const rows=await getAiSuggestions(id);const record=(value:unknown)=>typeof value==='object'&&value!==null&&!Array.isArray(value)?value as Record<string,unknown>:{};return <div className="mx-auto max-w-6xl space-y-6"><header><p className="text-xs font-black uppercase tracking-widest text-indigo-600">Asistencia responsable</p><h2 className="text-2xl font-black">Borradores y aprobación de IA</h2></header><AiHistoryGenerator gymnastId={id}/><AiSuggestionReview gymnastId={id} suggestions={rows.map(x=>({id:String(x.id),type:String(x.tipo),proposal:record(x.propuesta_editada||x.propuesta_original),rationale:String(x.fundamento),status:String(x.estado),date:String(x.created_at)}))}/></div>}
