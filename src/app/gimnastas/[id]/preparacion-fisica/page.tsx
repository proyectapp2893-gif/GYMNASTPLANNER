import Link from 'next/link';import {getPhysicalPreparationSummary} from '../../../../lib/gymnasts/server'
export default async function PhysicalPreparationPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const data=await getPhysicalPreparationSummary(id);return <div className="mx-auto max-w-7xl space-y-6"><header className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-widest text-indigo-600">Perfil físico</p><h2 className="text-2xl font-black">Preparación física individual</h2><p className="text-sm text-slate-500">Resumen de las últimas mediciones configurables; el historial completo permanece en Evaluaciones.</p></div><Link href={`/gimnastas/${id}/evaluaciones`} className="rounded-xl bg-indigo-600 px-4 py-3 text-sm font-bold text-white">Registrar evaluación</Link></header><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{data.latest.map(item=><article key={item.name} className="rounded-2xl border bg-white p-5"><p className="text-xs font-black uppercase text-slate-500">{item.name}</p><p className="mt-2 text-3xl font-black">{item.value} <span className="text-sm text-slate-500">{item.unit}</span></p><p className="mt-2 text-xs">Última medición: {item.date}</p></article>)}</div>{data.latest.length===0&&<div className="rounded-2xl border border-dashed p-10 text-center text-sm text-slate-500">Registra la primera batería de pruebas para construir el perfil físico.</div>}</div>}
