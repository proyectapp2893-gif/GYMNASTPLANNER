import SessionExecutionRecorder from '../../../../../../components/gymnasts/SessionExecutionRecorder'
import { getSessionForExecution } from '../../../../../../lib/gymnasts/server'
export default async function ExecuteSessionPage({params}:{params:Promise<{id:string;sessionId:string}>}){const {id,sessionId}=await params;const session=await getSessionForExecution(id,sessionId);return <div className="mx-auto max-w-7xl"><header className="mb-6"><p className="text-xs font-black uppercase tracking-widest text-indigo-600">Ejecución real</p><h2 className="text-2xl font-black">{session.objective}</h2><p className="text-sm text-slate-500">Registra únicamente lo realizado. La fórmula de carga se muestra de forma transparente.</p></header><SessionExecutionRecorder gymnastId={id} session={session}/></div>}
