import Link from 'next/link';import PedagogicalSessionEditor from '../../../../../../components/gymnasts/PedagogicalSessionEditor';import {getPedagogicalSession} from '../../../../../../lib/gymnasts/server'
export default async function PedagogyPage({params}:{params:Promise<{id:string;sessionId:string}>}){const {id,sessionId}=await params;const data=await getPedagogicalSession(id,sessionId);return <div className="mx-auto max-w-7xl space-y-6"><header><p className="text-xs font-black uppercase tracking-widest text-indigo-600">Proceso pedagógico completo</p><h2 className="text-2xl font-black">{data.objective}</h2><Link href={`/gimnastas/${id}/sesiones/${sessionId}/editar`} className="mt-2 inline-block text-sm font-bold text-indigo-600">← Volver a dosificación y ejercicios</Link></header><PedagogicalSessionEditor gymnastId={id} sessionId={sessionId} initial={data.contents}/></div>}
