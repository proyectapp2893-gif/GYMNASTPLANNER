import ProgressReviewManager from '../../../../components/gymnasts/ProgressReviewManager';import {getProgressReviewWorkspace} from '../../../../lib/gymnasts/server'
export default async function ProgressReviewsPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const data=await getProgressReviewWorkspace(id);return <div className="mx-auto max-w-6xl space-y-6"><header><p className="text-xs font-black uppercase tracking-widest text-violet-700">Fase 5 · Resultados</p><h2 className="text-2xl font-black">Revisiones de progreso</h2><p className="mt-2 text-sm text-slate-500">Síntesis periódica, prioridades siguientes y comunicación autorizada.</p></header><ProgressReviewManager athleteId={id} snapshot={data.snapshot} reviews={data.reviews}/></div>}
