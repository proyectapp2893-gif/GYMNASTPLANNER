import RestrictionsManager from '../../../../components/gymnasts/RestrictionsManager';import {getRestrictions} from '../../../../lib/gymnasts/server'
export default async function RestrictionsPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const rows=await getRestrictions(id);return <div className="mx-auto max-w-5xl"><header className="mb-6"><p className="text-xs font-black uppercase tracking-widest text-indigo-600">Seguridad individual</p><h2 className="text-2xl font-black">Molestias, restricciones y reintegro</h2></header><RestrictionsManager gymnastId={id} restrictions={rows.map(row=>({id:String(row.id),bodyArea:String(row.zona_corporal||'Sin zona'),intensity:row.intensidad_reportada===null?null:Number(row.intensidad_reportada),startDate:String(row.fecha_inicio),status:String(row.estado),adaptations:String(row.adaptaciones_temporales||''),returnAuthorized:Boolean(row.retorno_autorizado)}))}/></div>}
