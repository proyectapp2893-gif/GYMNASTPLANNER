import {z} from 'zod'
export const gymnastCompetitionSchema=z.object({competitionId:z.string().uuid().nullable(),name:z.string().trim().min(2).max(200).nullable(),date:z.string().date().nullable(),endDate:z.string().date().nullable(),city:z.string().trim().max(150).nullable(),level:z.string().trim().max(100).nullable(),category:z.string().trim().max(100).nullable(),rules:z.string().trim().max(300).nullable(),resultGoal:z.string().trim().max(1000).nullable(),executionGoal:z.string().trim().max(1000).nullable(),preparationDeadline:z.string().date().nullable(),requiredElements:z.array(z.string().trim().min(1).max(200)).max(100),optionalElements:z.array(z.string().trim().min(1).max(200)).max(100),priorities:z.array(z.string().trim().min(1).max(300)).max(30)}).refine(v=>Boolean(v.competitionId||(v.name&&v.date)),{message:'Selecciona una competencia o crea una con nombre y fecha'}).refine(v=>!v.endDate||!v.date||v.endDate>=v.date,{message:'La fecha final es inválida'})
