import {z} from 'zod'
export const technicalErrorSchema=z.object({elementId:z.string().uuid(),errorItemId:z.string().uuid(),date:z.string().date(),movementPhase:z.string().trim().max(120).nullable(),frequency:z.string().trim().max(80).nullable(),severity:z.number().int().min(1).max(5).nullable(),possibleCause:z.string().trim().max(1000).nullable(),correction:z.string().trim().max(1000).nullable(),verbalCue:z.string().trim().max(500).nullable(),subsequentResult:z.string().trim().max(1000).nullable(),trend:z.enum(['nuevo','persistente','disminucion','corregido','reaparecido'])})
export const progressionSchema=z.object({stepId:z.string().uuid(),status:z.enum(['pendiente','en_progreso','aprobado','suspendido','requiere_revision']),startDate:z.string().date().nullable(),masteryDate:z.string().date().nullable(),technicalQuality:z.number().int().min(1).max(5).nullable(),safety:z.number().int().min(1).max(5).nullable(),consistency:z.number().int().min(1).max(5).nullable(),understanding:z.number().int().min(1).max(5).nullable(),bodyControl:z.number().int().min(1).max(5).nullable(),correctExecutions:z.number().int().min(0),notes:z.string().trim().max(1500).nullable()}).refine(v=>v.status!=='aprobado'||[v.technicalQuality,v.safety,v.consistency,v.understanding,v.bodyControl].every(x=>x!==null),{message:'La aprobación requiere valorar calidad, seguridad, consistencia, comprensión y control corporal'})
