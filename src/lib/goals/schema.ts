import {z} from 'zod'
export const goalSchema=z.object({typeItemId:z.string().uuid().nullable(),description:z.string().trim().min(3).max(1000),startDate:z.string().date(),targetDate:z.string().date(),indicator:z.string().trim().max(300).nullable(),initialValue:z.number().finite().nullable(),expectedValue:z.number().finite().nullable(),progressPercent:z.number().min(0).max(100),status:z.enum(['pendiente','en_progreso','en_riesgo','alcanzado','parcialmente_alcanzado','pospuesto','cancelado']),priority:z.number().int().min(1).max(5),notes:z.string().trim().max(1500).nullable()}).refine(v=>v.targetDate>=v.startDate,{message:'La fecha objetivo no puede ser anterior a la inicial',path:['targetDate']})
