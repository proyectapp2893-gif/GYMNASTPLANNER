import {z} from 'zod'
export const feedbackSchema=z.object({sessionId:z.string().uuid().nullable(),elementId:z.string().uuid().nullable(),coachComment:z.string().trim().max(2000).nullable(),technicalCue:z.string().trim().max(1000).nullable(),explanation:z.string().trim().max(1500).nullable(),gymnastPerception:z.string().trim().max(1500).nullable(),understandingLevel:z.number().int().min(1).max(5).nullable(),nextFocus:z.string().trim().min(1).max(1000),visibleToFamily:z.boolean()}).refine(v=>Boolean(v.coachComment||v.technicalCue),{message:'Registra un comentario o indicación técnica'})
