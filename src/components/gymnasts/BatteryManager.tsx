'use client'
import {useState} from 'react'
import {useRouter} from 'next/navigation'
import {FlaskConical,Plus} from 'lucide-react'

type Test={id:string;name:string;unit:string}
type Battery={id:string;name:string;description:string;active:boolean;testIds:string[]}
export default function BatteryManager({tests,batteries}:{tests:Test[];batteries:Battery[]}){const router=useRouter();const [name,setName]=useState('');const [description,setDescription]=useState('');const [selected,setSelected]=useState<string[]>([]);const [message,setMessage]=useState('');const submit=async(event:React.FormEvent)=>{event.preventDefault();const response=await fetch('/api/baterias-pruebas',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,description:description||null,testIds:selected})});const result=await response.json() as {error?:string};setMessage(response.ok?'Batería creada':result.error||'No se pudo crear');if(response.ok){setName('');setDescription('');setSelected([]);router.refresh()}};const toggle=async(battery:Battery)=>{const response=await fetch('/api/baterias-pruebas',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:battery.id,active:!battery.active})});if(response.ok)router.refresh()};return <section className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-center gap-2"><FlaskConical className="text-indigo-600"/><div><h2 className="font-black">Baterías de pruebas físicas</h2><p className="text-sm text-slate-500">Agrupa pruebas configurables sin fijarlas en el código.</p></div></div><form onSubmit={submit} className="mt-5 grid gap-4 lg:grid-cols-2"><label className="grid gap-1 text-sm font-bold">Nombre<input required minLength={3} value={name} onChange={event=>setName(event.target.value)} className="rounded-xl border p-3"/></label><label className="grid gap-1 text-sm font-bold">Descripción<input value={description} onChange={event=>setDescription(event.target.value)} className="rounded-xl border p-3"/></label><fieldset className="grid gap-2 rounded-xl border p-4 sm:grid-cols-2 lg:col-span-2"><legend className="px-2 text-sm font-black">Pruebas incluidas</legend>{tests.map(test=><label key={test.id} className="flex items-center gap-2 text-sm"><input type="checkbox" checked={selected.includes(test.id)} onChange={event=>setSelected(current=>event.target.checked?[...current,test.id]:current.filter(id=>id!==test.id))}/>{test.name} <span className="text-slate-400">({test.unit})</span></label>)}</fieldset><button disabled={!name.trim()||selected.length===0} className="rounded-xl bg-indigo-600 p-3 font-bold text-white disabled:opacity-40"><Plus className="mr-2 inline h-4 w-4"/>Crear batería</button>{message&&<p role="status" className="self-center text-sm font-bold">{message}</p>}</form><div className="mt-6 grid gap-3 md:grid-cols-2">{batteries.map(battery=><article key={battery.id} className="rounded-xl border p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="font-black">{battery.name}</h3><p className="text-xs text-slate-500">{battery.description||'Sin descripción'} · {battery.testIds.length} pruebas</p></div><button onClick={()=>toggle(battery)} className={`rounded-lg px-3 py-1 text-xs font-black ${battery.active?'bg-emerald-100 text-emerald-700':'bg-slate-100 text-slate-500'}`}>{battery.active?'Activa':'Inactiva'}</button></div><p className="mt-3 text-sm text-slate-600">{battery.testIds.map(id=>tests.find(test=>test.id===id)?.name).filter(Boolean).join(' · ')}</p></article>)}</div></section>}
