'use client'
import {useState} from 'react'
import {FileVideo,Save} from 'lucide-react'

export default function EvidenceSettings({initialMaximumMb}:{initialMaximumMb:number}){const [maximumMb,setMaximumMb]=useState(initialMaximumMb);const [message,setMessage]=useState('');const [saving,setSaving]=useState(false);const save=async(event:React.FormEvent)=>{event.preventDefault();setSaving(true);const response=await fetch('/api/configuracion-evidencias',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({maximumMb})});const result=await response.json() as {error?:string};setMessage(response.ok?'Límite de evidencias actualizado':result.error||'No fue posible guardar');setSaving(false)};return <form onSubmit={save} className="rounded-2xl border border-indigo-200 bg-indigo-50 p-5"><div className="flex items-start gap-3"><FileVideo className="mt-1 text-indigo-600"/><div><h2 className="font-black text-indigo-950">Archivos de evidencia privados</h2><p className="text-sm text-indigo-800">El límite se valida antes y después de la carga firmada.</p></div></div><label className="mt-4 grid gap-1 text-sm font-bold text-indigo-950">Tamaño máximo por archivo (MB)<input required type="number" min="1" max="500" value={maximumMb} onChange={event=>setMaximumMb(Number(event.target.value))} className="rounded-xl border border-indigo-300 bg-white p-3"/></label><p className="mt-2 text-xs text-indigo-800">Formatos permitidos: MP4, MOV, JPEG, PNG y WebP.</p><button disabled={saving} className="mt-4 rounded-xl bg-indigo-700 px-4 py-3 font-bold text-white disabled:opacity-40"><Save className="mr-2 inline h-4 w-4"/>{saving?'Guardando…':'Guardar límite'}</button>{message&&<p role="status" className="mt-3 text-sm font-bold text-indigo-950">{message}</p>}</form>}
