import {CheckCircle2,ShieldAlert} from 'lucide-react'
export type OperationalStatus={athletes:number;athletesWithoutStage:number;athletesWithoutRecentCheckin:number;exercises:number;exercisesWithoutReadinessMetadata:number;approvedRules:number;overdueAdaptationPlans:number}
export default function OperationalReadinessPanel({status}:{status:OperationalStatus}){const checks=[['Etapas individuales',status.athletesWithoutStage===0,`${status.athletesWithoutStage} de ${status.athletes} pendientes`],['Check-ins vigentes',status.athletesWithoutRecentCheckin===0,`${status.athletesWithoutRecentCheckin} pendientes`],['Ejercicios clasificados',status.exercisesWithoutReadinessMetadata===0,`${status.exercisesWithoutReadinessMetadata} de ${status.exercises} incompletos`],['Reglas aprobadas',status.approvedRules>0,`${status.approvedRules} activas`],['Planes adaptativos al día',status.overdueAdaptationPlans===0,`${status.overdueAdaptationPlans} vencidos`]] as const;return <section className="rounded-2xl border border-slate-200 bg-slate-950 p-5 text-white"><h2 className="font-black">Preparación operativa · Fase 6</h2><p className="mt-1 text-sm text-slate-400">La inteligencia es tan confiable como la cobertura y vigencia de sus datos.</p><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{checks.map(([label,ok,detail])=><article key={label} className={`rounded-xl border p-3 ${ok?'border-emerald-700 bg-emerald-950/40':'border-amber-700 bg-amber-950/40'}`}>{ok?<CheckCircle2 className="h-5 w-5 text-emerald-400"/>:<ShieldAlert className="h-5 w-5 text-amber-400"/>}<p className="mt-2 text-sm font-black">{label}</p><p className="text-xs text-slate-300">{detail}</p></article>)}</div></section>}
